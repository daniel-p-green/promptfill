# PromptFill

Local-first prompt library + editor for LLM prompts with typed variables and dropdown selectors.

## Repo layout
- `web/` — Next.js web MVP

## Dev
```bash
cd web
npm install
npm run dev
```

## Tests
```bash
cd web
npm test
```
